<% title "Edit your account: " << @user.person.full_name %>

<%= semantic_form_for @user, :url => user_path(@user), :html => {:id => "edit_user"} do |form| %>

<%= render 'form', :form => form %>

<% end %>